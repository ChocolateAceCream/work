<% provide(:title, "Edit Territory") %>
<h1>Update Territory</h1>

<div class="row">
    <div class="col-md-6 col-md-offset-3">
        <%= form_for(@territory) do |f| %>
            <%= render 'fields', :f=>f %>
            <%= f.submit "Save changes", class: "btn btn-primary" %>
    <% end %>
    </div>
</div>
