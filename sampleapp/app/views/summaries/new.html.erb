<% provide(:title, 'New Summary')%>
<h1>New Summary</h1>
<div class="row">
    <div class="col-md-6 col-md-offset-3">
        <%= form_for(@summary) do |f| %>
        	<%= render 'fields', :f=>f %>
            <%= f.submit "Create", class: "btn btn-primary" %>
            </p>
        <% end %>
    </div>

</div>

