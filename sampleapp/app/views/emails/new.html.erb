<% provide(:title, 'New email server')%>
<h1>new email server</h1>
<div class="row">
    <div class="col-md-6 col-md-offset-3">
        <%= form_for(@email) do |f| %>
            <%= render 'fields', :f=> f %>
            <%= f.submit "Create new email server", class: "btn btn-primary" %>
    <% end %>
    </div>

</div>