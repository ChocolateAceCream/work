<% provide(:title, "Edit Email Servers") %>
<h1>Update Email Servers</h1>

<div class="row">
    <div class="col-md-6 col-md-offset-3">
        <%= form_for(@email) do |f| %>
            <%= render 'fields', :f=>f %>
            <%= f.submit "Save changes", class: "btn btn-primary" %>
    <% end %>
    </div>
</div>
